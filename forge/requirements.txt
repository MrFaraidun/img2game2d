Pillow>=9.0.0
numpy>=1.21.0
# Optional — for schema validation (falls back to stdlib if missing)
jsonschema>=4.0.0
# Optional — for best background removal
# rembg>=2.0.0
# Optional — for AI frame generation
# openai>=1.0.0
